name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    #package dependencies
  - numpy
  - math    
  - copy
  - networkx
  - matplotlib
  - random

    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov

