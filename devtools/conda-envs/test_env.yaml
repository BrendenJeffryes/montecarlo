name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    #package dependencies
  - numpy 
  - networkx
  - matplotlib

    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov

